import type { EventLookupResult } from "./types.js";

/** Placeholder: Bandsintown API requires artist-scoped flows; not wired in this build. */
export const searchBandsintownEvents = async (): Promise<EventLookupResult[]> => [];
