import type { EventLookupResult } from "./types.js";

/** Placeholder: Songkick public API is not available without separate licensing. */
export const searchSongkickEvents = async (): Promise<EventLookupResult[]> => [];
